# systemctl --user start mpd

# bind_to_address ""
# port "6600"

# MPD 在这个文件夹中扫描音乐
music_directory    "~/Music"
auto_update "yes"

# 日志
# 如果使用 systemd 运行 MPD，删除下面这一行，让日志直接写入 systemd。
# log_file           "~/.cache/mpd/log"

# 音乐数据库
db_file            "~/.local/state/mpd/database"
# 播放列表存储文件夹,需要 `mkdir ~/.config/mpd/playlists`
playlist_directory "~/.local/state/mpd/playlists"
# MPD 存储进程 ID（PID）的文件
pid_file           "~/.local/state/mpd/pid"
# 记录 MPD 当前状态 `mkdir ~/.local/state/mpd`
state_file         "~/.local/state/mpd/state"
# 标签数据库
sticker_file       "~/.local/state/mpd/sticker.sql"

audio_output {
  type            "pipewire"
  name            "PipeWire Sound Server"
}
